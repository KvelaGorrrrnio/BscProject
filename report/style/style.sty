%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Package for changing to a nicer font
\usepackage [T1]{fontenc}
\usepackage[utf8]{inputenc}
%\usepackage{lmodern}
%\usepackage{mathpazo}

%%%%%%%%%%%%%%%%%%%%
% Paper styling
\usepackage[margin=3cm]{geometry}
\raggedbottom

%%%%%%%%%%%%%%%%%%%%
% Frontpage

\usepackage{wallpaper}

%%%%%%%%%%%%%%%%%%%%
% Figures

% Plots
\usepackage{pgfplots}

% Wrapping figures
\usepackage{wrapfig}

% Subcaptions
\usepackage{subcaption}

% Placement
\usepackage{float}

% Graphics / Images
\usepackage{graphicx}
\graphicspath{{figures/}}

% Tikz
\usepackage{tikz}
\usepackage{textcomp}
\usetikzlibrary{shapes,arrows,positioning}
\tikzset{%
  stmt/.style     = {draw, thick, rectangle, minimum height = 2em,
    minimum width = 3em},
  test/.style     = {draw, thick, diamond, minimum height = 3em,
    minimum width = 3em},
  assert/.style   = {draw, thick, circle, minimum height = 3em,
    minimum width = 3em},
  input/.style    = {coordinate}, % Input
  output/.style   = {coordinate} % Output
}

%%%%%%%%%%%%%%%%%%%%
% Listings
\usepackage{listings}
\usepackage{color,xcolor}

% Set path to source
%\lstset{inputpath=../../src/src/}
%\lstset{inputpath=./}

% Colors
\definecolor{graybg}{RGB}{246,247,248}
\definecolor{codeblue}{RGB}{15,126,227}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{KU-red}{RGB}{144,26,30}

% Languages
\lstloadlanguages{sh,haskell}
\lstdefinelanguage{javascript}{
  keywords={typeof, new, true, false, catch, function, return, null, catch, switch, var, if, in, while, do, else, case, break, continue},
  ndkeywords={const, class, extends, export, boolean, throw, implements, import, from, default, this},
  sensitive=false,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]"
}
\lstdefinelanguage{srl}{
  keywords={init, free, swap, push, pop, skip, from, do, until, if, then, else, fi},
  ndkeywords={list, int},
  sensitive=false,
  comment=[l]{//},
}
\lstdefinelanguage{rl}{
  keywords={init, free, swap, push, pop, skip, from, goto, if, fi},
  ndkeywords={list, int, entry, exit},
  sensitive=false,
  comment=[l]{//},
}

% Styling
\lstdefinestyle{thesis}{
  basicstyle        = \fontsize{9}{9}\ttfamily,
  backgroundcolor   = \color{graybg},
  commentstyle      = \color{gray},
  keywordstyle      = \color{codeblue},
  ndkeywordstyle    = \color{magenta},
  stringstyle       = \color{purple},
  numberstyle       = \tiny\color{gray},
  numbers           = left,
  numbersep         = 5pt,
  breakatwhitespace = false,
  breaklines        = true,
  keepspaces        = true,
  showstringspaces  = false,
  showtabs          = false,
  tabsize           = 2,
  captionpos        = b
}
\lstset{style=thesis}
\lstdefinestyle{appendix}{
  captionpos = t
}

%%%%%%%%%%%%%%%%%%%%
% Math
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\setcounter{MaxMatrixCols}{20}

%%%%%%%%%%%%%%%%%%%%
% Quotes
\usepackage{dirtytalk}

%%%%%%%%%%%%%%%%%%%%
% Links
\usepackage[colorlinks = true,
            linkcolor = black,
            urlcolor  = blue,
            citecolor = blue,
            anchorcolor = blue]{hyperref}


%%%%%%%%%%%%%%%%%%%%
% Referencing
\usepackage[
  backend=biber,
  style=numeric,
  sorting=ynt,
  citestyle=numeric % Change this for citation styles
]{biblatex}
\addbibresource{references.bib}

%%%%%%%%%%%%%%%%%%%%
% Appendices
\usepackage[toc,page]{appendix}

%%%%%%%%%%%%
% Custom commands

% OperatorEq
\newcommand{\pluseq}{\mathrel{+}=}
\newcommand{\minuseq}{\mathrel{-}=}
\newcommand{\xoreq}{\mathrel{^}=}
\newcommand{\timeseq}{\mathrel{*}=}
\newcommand{\diveq}{\mathrel{/}=}

% Listing
\newcommand{\shcmd}{\lstinline[language=sh]}

% Todo command
\newcommand{\todo}[1]{{\color{red}\textbf{TODO:{ #1 }}}}

% Inline icons
\DeclareRobustCommand{\inlineicon}[1]{%
  \begingroup\normalfont
  \includegraphics[height=\fontcharht\font`\B]{#1}%
  \endgroup
}
